<!DOCTYPE html>

<title>New Artisan</title>

{% block body %}
    <h1>Create new Artisan</h1>

    {{ include('artisan/_form.html.twig') }}

    <a href="{{ path('app_artisan_index') }}">back to list</a>
{% endblock %}
